{% extends "plasmid/base.html.twig" %}

{% block title %}Plasmid{% endblock %}

{% block breadcrumb %}
    {{ parent() }}
    <li class="active">Plasmids list</li>
{% endblock %}

{% block plasmid_body %}
    {% if app.user.isInTeam %}
        <div>
            <a class="btn btn-default" href="{{ path('plasmid_add') }}">
                <span class="glyphicon glyphicon-plus" aria-hidden="true"></span> Add a plasmid
            </a>
        </div>
    {% endif %}

    <h2 class="sub-header">Plasmids list</h2>

    <div class="row">
        <form id="plasmid-search-form" method="get">
            <div class="col-md-3">
                <input type="text" id="plasmid-search-field" name="q" class="form-control" placeholder="Search for..." autocomplete="off" value="{{ query }}">
            </div>

            <div class="col-md-2">
                <select id="plasmid-team-field" name="team" class="form-control col-md-3">
                    {% for team in app.user.teams %}
                        <option value="{{ team.id }}"{{ app.user.isFavoriteTeam(team) ? 'selected="selected"' }}>{{ team.name }}</option>
                    {% endfor %}
                </select>
            </div>

            <div class="col-md-1">
                <button id="plasmid-search-button" class="btn btn-default" type="submit">
                    <span class="glyphicon glyphicon-search" aria-hidden="true"></span> Search
                </button>
            </div>
        </form>
    </div>

    {{ list.content|raw }}
{% endblock %}

{% block javascripts %}
    {{ parent() }}

    <script>
        $(document).ready(function(){
            var processing = false;
            var search = $('#plasmid-search-field');
            var team = $('#plasmid-team-field');

            search.keyup(function() {
                history.replaceState('', '', Routing.generate('plasmid_index', { q: search.val(), team: team.val(), p: 1 }));

                delay(function(){
                    $.ajax({
                        type: 'GET',
                        url: Routing.generate('plasmid_index_ajax', { q: search.val(), team: team.val(), p: 1 }),
                        dataType: 'html',
                        delay: 400,
                        beforeSend: function() {
                            if (processing) {
                                return false;
                            } else {
                                processing = true;
                            }
                        },
                        success: function (html) {
                            $('#plasmid-list').replaceWith(html);
                            processing = false;
                        }
                    });
                }, 400 );
            });

            team.change(function() {
                history.replaceState('', '', Routing.generate('plasmid_index', { q: search.val(), team: team.val(), p: 1 }));
                $.ajax({
                    type: 'GET',
                    url: Routing.generate('plasmid_index_ajax', { q: search.val(), team: team.val(), p: 1 }),
                    dataType: 'html',
                    delay: 400,
                    beforeSend: function() {
                        if (processing) {
                            return false;
                        } else {
                            processing = true;
                        }
                    },
                    success: function (html) {
                        $('#plasmid-list').replaceWith(html);
                        processing = false;
                    }
                });
            });
        });
    </script>
{% endblock %}
