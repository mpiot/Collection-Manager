{{ include('plasmid/_delete_confirmation.html.twig') }}
<form action="{{ path('plasmid_delete', { 'id': plasmid.id, 'slug': plasmid.slug }) }}" method="post" data-confirmation="true">
    <input type="hidden" name="token" value="{{ csrf_token('plasmid_delete') }}" />
    <div class="btn-group btn-group-xs" role="group">
        {% block btn %}
        {% endblock %}

        <button type="submit" class="btn btn-danger">
            <span class="glyphicon glyphicon-trash" aria-hidden="true"></span> Delete
        </button>
    </div>
</form>
