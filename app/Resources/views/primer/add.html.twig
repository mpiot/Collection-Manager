{% extends "primer/base.html.twig" %}

{% block title %}Add a primer{% endblock %}

{% block primer_body %}
    <div>
        <a class="btn btn-default" href="{{ path('primer_index') }}">
            <span class="glyphicon glyphicon-chevron-left"></span> Primers list
        </a>
    </div>

    <h2 class="sub-header">Add a primer</h2>

    {{ form_start(form) }}
        {{ form_widget(form) }}

        <input type="submit" class="btn btn-default pull-right" value="Add"/>
    {{ form_end(form) }}
{% endblock %}
