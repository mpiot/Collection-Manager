{% extends "type/base.html.twig" %}

{% block title %}{{ parent() }}:edit{% endblock %}

{% block type_body %}
    <div>
        <a class="btn btn-default" href="{{ path('type_index') }}">
            <span class="glyphicon glyphicon-chevron-left"></span> Types List
        </a>
    </div>

    <h2 class="sub-header">Edit a type</h2>

    {{ form_start(form) }}
        {{ form_widget(form) }}

        <input type="submit" class="btn btn-default pull-right" value="Edit"/>
    {{ form_end(form) }}
{% endblock %}
