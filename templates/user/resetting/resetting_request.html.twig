{% extends 'base.html.twig' %}

{% block breadcrumb %}
    {{ parent() }}
    <li class="breadcrumb-item active">Ask new password</li>
{% endblock %}

{% block body %}
    {{ form_start(form) }}

        {{ form_widget(form) }}

        <div class="form-group">
            <div class="col-sm-offset-2 col-sm-10">
                <input type="submit" class="btn btn-secondary" value="Ask a new password"/>
            </div>
        </div>
    {{ form_end(form) }}
{% endblock %}
