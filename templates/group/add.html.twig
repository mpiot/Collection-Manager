{% extends "base.html.twig" %}

{% block title %}Add a group{% endblock %}

{% block breadcrumb %}
    {{ parent() }}
    <li><a href="{{ path('group_index') }}">Groups</a></li>
    <li class="active">Add</li>
{% endblock %}

{% block body %}
    <h1 class="page-header">Add a group</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}
